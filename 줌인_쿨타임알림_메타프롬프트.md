# 메타 프롬프트: "줌인" 프로그램 — 파티 쿨타임/버프 알림 기능

> 이 문서는 다른 Claude 세션(예: Claude Code)에 그대로 붙여넣어 구현을 시작할 때 쓰는 프롬프트입니다.

---

## 역할 지시

당신은 기존 "줌인" 프로그램에 새 기능을 추가하는 엔지니어입니다. 먼저 프로젝트 코드베이스를 탐색해 언어/프레임워크, 기존 화면 캡처·이미지 인식 로직 유무, 네트워크 통신 계층 유무를 파악한 뒤 아래 요구사항을 그 구조에 맞게 구현하세요. 확실하지 않은 부분은 추측해서 진행하지 말고 질문하세요.

## 배경

파티(딜러/서포터) 플레이 중 서로의 스킬 쿨타임·버프 온오프 상태를 실시간으로 확인하기 어렵다는 문제에서 출발했습니다. 기존에는 디스코드 화면 공유(방송)로 이를 확인했지만 해상도가 낮아 인식이 부정확했습니다.

## 목표

이미지 인식으로 스킬 쿨타임/버프 상태를 자동 감지하고, 필요한 시점에 알림을 보내는 기능을 "줌인"에 구현합니다.

## 검토된 두 가지 방식

### 방식 A — 디스코드 방송 캡처
디스코드에 공유된 화면을 캡처해 인식. 한 사람만 켜도 파티 전체 정보를 볼 수 있지만, 압축된 화면이라 해상도가 낮아 인식 정확도가 떨어짐.

### 방식 B — 로컬 실행 + 양방향 공유 (우선 검토)
파티원 각자가 자기 PC에서 프로그램을 실행해 자신의 고해상도 원본 화면에서 직접 쿨타임/버프를 인식하고, 그 결과를 네트워크로 서로 공유. 인식 정확도가 높고, 디스코드 해상도 문제 자체가 사라지며, 각자 개인 화면에서 파티원 상태를 확인할 수 있음.

**→ 대화에서 더 선호된 방향이므로 방식 B를 1차 구현 대상으로 진행하고, 방식 A는 필요 시 보조 옵션으로 고려하세요.**

## 구현 전 확인 사항

1. 기존 "줌인" 코드베이스의 언어/프레임워크, 이미 존재하는 이미지 인식(쿨타임 감지 등) 로직 및 재사용 가능 여부
2. 화면 캡처 대상 영역(스킬바/버프바 등)을 어떻게 지정·설정할지
3. 방식 B 구현에 필요한 네트워크 계층 유무 — 없다면 P2P 또는 로컬 서버 중계 중 프로젝트 구조에 맞는 방식 제안

## 기능 요구사항

- [ ] 지정된 화면 영역을 주기적으로 캡처해 쿨타임 완료/버프 온오프 상태를 이미지 인식으로 판별
- [ ] 상태 변화 감지 시 알림(사운드/팝업 등) 발생
- [ ] 감지된 상태를 같은 파티 내 다른 사용자에게 실시간 전송하는 네트워크 계층 (방식 B)
- [ ] 각 사용자가 자기 화면에서 파티원 전체 상태를 한눈에 볼 수 있는 UI

## 우선순위

1차: 방식 B 프로토타입 (로컬 인식 → 감지 → 알림, 단일 사용자 기준으로 우선 동작 확인)
2차: 네트워크 공유 계층 붙여서 양방향 동작 확인
3차 (선택): 방식 A를 보조 옵션으로 검토

## 참고

소규모 사이드 프로젝트로, 처음에는 같은 파티원들끼리 쓰는 것을 목표로 합니다. 과한 확장성보다 빠르게 동작하는 프로토타입을 우선하세요.
